server:
  port: 8888

spring:
  application:
    name: eadhub-config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///home/matheus/Área de trabalho/eadhub/config-repo
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}

management:
  endpoints:
    web:
      exposure:
        include: health
